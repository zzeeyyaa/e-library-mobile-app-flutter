import 'package:flutter/material.dart';

class ProfileView extends StatelessWidget {
  const ProfileView({super.key});

  static Widget page() => const ProfileView();

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
